{
  "name": "keyboard-shortcut-manager",
  "version": "1.0.0",
  "description": "Chrome keyboard shortcut manager extension",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
